<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gradient background with rounded left corners and stroke -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:endColor="#02041A"
                android:startColor="#02041A"
                android:type="linear" />
            <corners
                android:bottomLeftRadius="@dimen/_10sdp"
                android:bottomRightRadius="@dimen/_10sdp"
                android:topLeftRadius="@dimen/_10sdp"
                android:topRightRadius="@dimen/_10sdp" />
            <stroke
                android:width="1dp"
                android:color="#868CFF" />
        </shape>
    </item>

    <!-- Mask to remove stroke on the right side -->
    <item android:right="-3dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Transparent right margin -->
    <item android:right="10dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

</layer-list>