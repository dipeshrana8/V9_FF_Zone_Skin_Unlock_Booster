<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape with curved bottom-right corner -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#02041A" /> <!-- Change to your color -->
            <corners
                android:bottomLeftRadius="@dimen/_10sdp"
                android:bottomRightRadius="@dimen/_10sdp"
                android:topLeftRadius="@dimen/_10sdp"
                android:topRightRadius="@dimen/_10sdp" />
        </shape>
    </item>

    <!-- Transparent margin on the right -->
    <item android:right="10dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</layer-list>
